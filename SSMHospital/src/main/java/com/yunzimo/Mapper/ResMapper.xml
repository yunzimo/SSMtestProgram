<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yunzimo.Mapper.ResMapper">
    <!-- namespace要和接口的全类名相同 -->

    <select id="getAllResources" resultType="resources">
        select * from resources;
    </select>
</mapper>